Assign one structural role per signal with confidence, status and the evidence IDs you used. If a name hint exists, treat it as weak evidence (confidence cannot exceed 0.6 on a name alone).

Artifacts:
{{ payload_json }}

Return one JSON object: {"roles": [...], "notes": "..."}.
