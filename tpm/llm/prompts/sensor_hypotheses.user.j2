Propose instrument and unit-operation hypotheses for the signals below. One entry per signal in the catalog (skip constants, counters, timestamps and identifiers). Cite evidence IDs from the artifacts; list alternatives; state uncertainty.

Artifacts:
{{ payload_json }}

Return one JSON object: {"hypotheses": [...], "process_hypothesis": "...", "process_confidence": 0-1, "uncertainty": [...]}.
